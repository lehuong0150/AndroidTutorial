<!-- res/values/styles.xml -->
<resources>
    <!-- Base Calculator Button Style -->
    <style name="CalculatorButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:layout_rowWeight">1</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:elevation">2dp</item>
    </style>

    <!-- Number Buttons -->
    <style name="CalculatorButton.Number">
        <item name="android:background">@drawable/bg_button_number</item>
        <item name="android:textColor">#000000</item>
    </style>

    <!-- Function Buttons (AC, +/-, %) -->
    <style name="CalculatorButton.Function">
        <item name="android:background">@drawable/bg_button_function</item>
        <item name="android:textColor">#000000</item>
    </style>

    <!-- Operator Buttons (+, -, ×, ÷) -->
    <style name="CalculatorButton.Operator">
        <item name="android:background">@drawable/bg_button_operator</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <!-- Zero Button (spans 2 columns) -->
    <style name="CalculatorButton.Zero">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_columnWeight">2</item>
        <item name="android:layout_rowWeight">1</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:background">@drawable/bg_button_number</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">20sp</item>
        <item name="android:elevation">2dp</item>
    </style>

    <!-- Equals Button -->
    <style name="CalculatorButton.Equals">
        <item name="android:background">@drawable/bg_button_equals</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>
</resources>
